
/**
 * Needed variables and eventListeners
 * Edit at your own risk!
 */
var favsList = [];
var count = 0;
var currentIndex = -1;
addButton.addEventListener("click", addItem);
previousButton.addEventListener("click", scroll);
nextButton.addEventListener("click", scroll);

/**
 * TODO: Complete the addItem function
 * which adds an item from the listInput input
 * box to the favsList array
 */
function addItem(){


    

}

/**
 * TODO:  Complete the displayItems function
 * which displays the items in the favsList array
 * in the listContainer.  Maker sure you append
 * each new item to the listContainer using
 * listContainer.append
 */
function displayItems(){

}

/**
 * TODO: Complete the scroll function.  If event.target.id == "next",
 * you must highlight the next element in the list.  If event.target.id = "previous", 
 * you must lightlight the previous element in the list.  You must force the user to 
 * stay within the bounds of the favsList array
 * @param {click event} event 
 */
function scroll(event){


}